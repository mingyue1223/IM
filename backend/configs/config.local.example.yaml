# Local development configuration for backend/docker-compose.yaml.
server:
  port: 18080
  pprof_port: 16060
  ws_path: "/ws"
  upload_dir: "./uploads"

mysql:
  host: "localhost"
  port: 13306
  user: "goim"
  password: "goim123"
  db_name: "goim"

redis:
  addr: "localhost:16379"
  password: ""
  db: 0

rabbitmq:
  url: "amqp://guest:guest@localhost:5673/"

jwt:
  secret: "goim-local-development-secret-change-before-production"
  access_exp_hours: 2
  refresh_exp_days: 7

file:
  max_size_mb: 50
  allowed_exts: ["jpg", "png", "gif", "mp4"]
  upload_dir: "./uploads"

moment:
  big_user_friend_threshold: 500
  timeline_max_len: 1000
  like_persist_batch_size: 200
  like_persist_flush_ms: 500
  like_cache_ttl_hours: 168
