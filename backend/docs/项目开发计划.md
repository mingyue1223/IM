# GoIM 实施计划

**目标：** 使用 Go 构建一个高并发即时通讯系统（GoIM），支持私聊、群聊、朋友圈动态和 AI 助手，展示 Redis 优先 + MQ 异步持久化架构。

**架构：** 单进程 Go 单体应用，包含 Gin HTTP 服务器 + gorilla/websocket 实时服务器 + RabbitMQ 消费者。Redis 作为热存储层（inbox/outbox 有序集合），MySQL 作为冷持久层（通过 MQ 异步），RabbitMQ 解耦写入路径和读取路径。

**技术栈：** Go 1.22+、Gin、gorilla/websocket、MySQL 8.0、Redis 7、RabbitMQ、go-redis/redis、stretchr/testify、golang-jwt/jwt、bcrypt、amqp091-go

## 全局约束

- Go 1.22+（适用时使用 range-over-func 迭代器）
- 所有 MySQL 写入必须通过 MQ（Redis 优先模式）
- 私聊使用推送模型（每用户 inbox 有序集合），群聊使用拉取模型（每群 outbox 有序集合）
- 发送者不能看到接收者的已读状态（微信隐私设计）
- 所有 Redis Lua 脚本必须是原子性的——不允许在没有 Lua 的情况下进行多步 Redis 操作
- WebSocket 消息格式：JSON，包含 `type` + `data` 字段
- 消息 ID：Redis INCR `msg_id_global`（非 UUID，非 MySQL 自增）
- inbox/outbox 存活时间：通过 ZREMRANGEBYSCORE 设置 3 天 + 通过 ZREMRANGEBYRANK 设置最大数量上限
- 会话 ID 格式：私聊 = `p_{较小ID}_{较大ID}`，群聊 = `g_{群ID}`
- AI 系统用户 ID = 0（AI_SYSTEM_ID）
- 群组最大成员数 = 500

---

## 文件结构

```
goim/
├── cmd/
│   └── server/
│       └── main.go                    # 入口点：启动 HTTP + WS + MQ 消费者
│
├── internal/
│   ├── config/
│   │   └── config.go                  # 应用配置（基于环境变量/文件）
│   │
│   ├── model/
│   │   ├── user.go                    # User、FriendRequest、Friendship 结构体
│   │   ├── message.go                 # PrivateMessage、GroupMessage、WsMessage 结构体
│   │   ├── group.go                   # Group、GroupMember 结构体
│   │   ├── moment.go                  # Moment、MomentLike、MomentComment 结构体
│   │   ├── ai.go                      # AISummary、AIProfileItem 结构体
│   │   └── common.go                  # 共享类型（ConvID、MsgType 常量）
│   │
│   ├── repository/
│   │   ├── mysql_repo.go              # MySQL CRUD 操作（所有表）
│   │   ├── redis_repo.go              # Redis 操作（inbox/outbox/Lua 脚本）
│   │   └── mq_repo.go                 # RabbitMQ 发布辅助函数
│   │
│   ├── conn/
│   │   ├── manager.go                 # ConnectionManager（sync.Map）
│   │   ├── client.go                  # ClientConnection + ReadPump/WritePump
│   │   └── handler.go                 # handleMessage 分发器
│   │
│   ├── service/
│   │   ├── auth_service.go            # JWT 认证、注册、登录
│   │   ├── msg_service.go             # 私聊和群聊消息发送/接收
│   │   ├── friend_service.go          # 好友请求、接受、列表
│   │   ├── group_service.go           # 群组 CRUD、成员、踢出、禁言
│   │   ├── moment_service.go          # 朋友圈发布、动态流、点赞、评论
│   │   ├── ai_service.go              # AI 聊天、记忆层、LLM 集成
│   │   ├── msg_op_service.go          # 撤回、删除、搜索
│   │   └── user_service.go            # 个人资料、黑名单、设置
│   │
│   ├── consumer/
│   │   ├── private_msg_consumer.go    # MQ 消费者：写入 inbox + 推送 WS + 持久化 MySQL
│   │   ├── group_msg_consumer.go      # MQ 消费者：写入 outbox + 推送在线 + 持久化 MySQL
│   │   ├── moment_push_consumer.go    # MQ 消费者：写入时间线有序集合
│   │   ├── like_consumer.go           # MQ 消费者：持久化点赞到 MySQL
│   │   ├── comment_consumer.go        # MQ 消费者：持久化评论到 MySQL
│   │   ├── ai_consumer.go             # MQ 消费者：持久化 AI 摘要/用户画像
│   │
│   ├── api/
│   │   ├── router.go                  # Gin 路由设置
│   │   ├── auth_handler.go            # POST /auth/register、/auth/login、/auth/refresh
│   │   ├── friend_handler.go          # 好友 CRUD 端点
│   │   ├── group_handler.go           # 群组 CRUD 端点
│   │   ├── moment_handler.go          # 朋友圈 CRUD + 点赞/评论 端点
│   │   ├── msg_handler.go             # 历史记录、搜索端点
│   │   ├── file_handler.go            # 文件上传端点
│   │   ├── user_handler.go            # 个人资料、黑名单、设置端点
│   │   ├── ai_handler.go              # AI 清除上下文端点
│   │
│   ├── middleware/
│   │   └── auth_middleware.go          # Gin 路由的 JWT 认证中间件
│   │
│   ├── redis/
│   │   ├── lua_scripts.go             # 所有 Lua 脚本源码字符串 + 加载器
│   │   └── lua_private_msg.go         # 私聊消息 Lua：好友检查 + 在线 + msgID + 去重
│   │   ├── lua_group_msg.go           # 群聊消息 Lua：成员检查 + 禁言检查 + 序号
│   │   ├── lua_inbox_mark_read.go     # 将 inbox 中会话标记为已读（readStatus 0→1）
│   │   ├── lua_revoke_msg.go          # 撤回消息：ZREM 原消息 + ZADD 撤回替换
│   │
│   ├── ws/
│   │   ├── upgrade.go                 # WebSocket 升级 + JWT 验证 + 踢出旧连接
│   │   └── protocol.go                # WsMessage 类型常量 + 编码/解码辅助函数
│   │
│   ├── llm/
│   │   ├── client.go                  # LLMClient 接口
│   │   ├── openai_client.go           # OpenAI 实现
│   │   ├── domestic_client.go         # 国内 LLM 实现
│   │
│   └── infra/
│       ├── mysql.go                   # MySQL 连接设置
│       ├── redis.go                   # Redis 连接设置
│       ├── rabbitmq.go                # RabbitMQ 连接 + 通道设置
│       └── cleanup.go                 # 定期 inbox/outbox/时间线清理任务
│
├── scripts/
│   ├── migrations/
│   │   ├── 001_create_users.sql
│   │   ├── 002_create_friendships.sql
│   │   ├── 003_create_groups.sql
│   │   ├── 004_create_messages.sql
│   │   ├── 005_create_moments.sql
│   │   ├── 006_create_misc.sql
│   │   ├── 007_create_ai.sql
│   │
│   └── lua/
│       ├── private_msg_check.lua
│       ├── group_msg_check.lua
│       ├── inbox_mark_read.lua
│       ├── revoke_msg.lua
│
├── configs/
│   ├── config.yaml                    # 默认配置
│   ├── config.test.yaml               # 测试配置
│
├── uploads/                           # 本地文件存储（git 忽略）
│
├── go.mod
├── go.sum
├── Makefile
├── Dockerfile
├── docker-compose.yaml                # 开发环境 MySQL + Redis + RabbitMQ
└── .gitignore
```

---

## 阶段 0：项目脚手架与基础设施

### 任务 1：初始化 Go 项目及依赖

**文件：**
- 创建：`go.mod`、`Makefile`、`.gitignore`、`configs/config.yaml`、`configs/config.test.yaml`
- 创建：`internal/config/config.go`
- 创建：`docker-compose.yaml`

**接口：**
- 产出：包含 MySQL/Redis/RabbitMQ/Server 设置的 `Config` 结构体、`LoadConfig()` 函数

- [ ] **步骤 1：初始化 Go 模块并安装依赖**

```bash
cd goim
go mod init github.com/yourname/goim
go get github.com/gin-gonic/gin@v1.9.1
go get github.com/gorilla/websocket@v1.5.3
go get github.com/redis/go-redis/v9@v9.5.1
go get github.com/golang-jwt/jwt/v5@v5.2.1
go get github.com/amqp091-go/amqp091-go@v1.9.0
go get golang.org/x/crypto@v0.23.0  # bcrypt
go get github.com/stretchr/testify@v1.9.0
go get go.uber.org/zap@v1.27.0  # 结构化日志
go get gopkg.in/yaml.v3           # 配置解析
```

- [ ] **步骤 2：创建配置结构体和加载器**

创建 `internal/config/config.go`：
```go
package config

import (
    "os"
    "gopkg.in/yaml.v3"
)

type Config struct {
    Server   ServerConfig   `yaml:"server"`
    MySQL    MySQLConfig    `yaml:"mysql"`
    Redis    RedisConfig    `yaml:"redis"`
    RabbitMQ RabbitMQConfig `yaml:"rabbitmq"`
    JWT      JWTConfig      `yaml:"jwt"`
    LLM      LLMConfig      `yaml:"llm"`
    File     FileConfig     `yaml:"file"`
}

type ServerConfig struct {
    Port         int    `yaml:"port"`
    WsPath       string `yaml:"ws_path"`
    UploadDir    string `yaml:"upload_dir"`
}

type MySQLConfig struct {
    Host     string `yaml:"host"`
    Port     int    `yaml:"port"`
    User     string `yaml:"user"`
    Password string `yaml:"password"`
    DBName   string `yaml:"db_name"`
}

type RedisConfig struct {
    Addr     string `yaml:"addr"`
    Password string `yaml:"password"`
    DB       int    `yaml:"db"`
}

type RabbitMQConfig struct {
    URL string `yaml:"url"`
}

type JWTConfig struct {
    Secret          string `yaml:"secret"`
    AccessExpHours  int    `yaml:"access_exp_hours"`
    RefreshExpDays  int    `yaml:"refresh_exp_days"`
}

type LLMConfig struct {
    Provider string `yaml:"provider"` // "openai" 或 "domestic"
    APIKey   string `yaml:"api_key"`
    BaseURL  string `yaml:"base_url"`
    Model    string `yaml:"model"`
}

type FileConfig struct {
    MaxSizeMB   int      `yaml:"max_size_mb"`
    AllowedExts []string `yaml:"allowed_exts"`
    UploadDir   string   `yaml:"upload_dir"`
}

func LoadConfig(path string) (*Config, error) {
    data, err := os.ReadFile(path)
    if err != nil {
        return nil, err
    }
    var cfg Config
    if err := yaml.Unmarshal(data, &cfg); err != nil {
        return nil, err
    }
    return &cfg, nil
}
```

- [ ] **步骤 3：创建配置 YAML 文件**

创建 `configs/config.yaml`：
```yaml
server:
  port: 8080
  ws_path: "/ws"
  upload_dir: "./uploads"

mysql:
  host: "localhost"
  port: 3306
  user: "goim"
  password: "goim123"
  db_name: "goim"

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

rabbitmq:
  url: "amqp://guest:guest@localhost:5672/"

jwt:
  secret: "goim-dev-secret-change-in-production"
  access_exp_hours: 2
  refresh_exp_days: 7

llm:
  provider: "openai"
  api_key: "${OPENAI_API_KEY}"
  base_url: "https://api.openai.com/v1"
  model: "gpt-4o-mini"

file:
  max_size_mb: 50
  allowed_exts: ["jpg", "png", "gif", "mp4"]
  upload_dir: "./uploads"
```

- [ ] **步骤 4：为开发基础设施创建 docker-compose.yaml**

```yaml
version: "3.8"
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: goim
      MYSQL_USER: goim
      MYSQL_PASSWORD: goim123
    ports: ["3306:3306"]
    volumes: [mysql_data:/var/lib/mysql]

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]

  rabbitmq:
    image: rabbitmq:3-management-alpine
    ports: ["5672:5672", "15672:15672"]

volumes:
  mysql_data:
```

- [ ] **步骤 5：创建 Makefile**

```makefile
.PHONY: run test docker migrate clean

run:
	go run cmd/server/main.go -c configs/config.yaml

test:
	go test ./... -v

docker:
	docker-compose up -d

migrate:
	for f in scripts/migrations/*.sql; do mysql -u goim -pgoim123 goim < $$f; done

clean:
	docker-compose down -v
	rm -rf uploads/
```

- [ ] **步骤 6：创建 .gitignore**

```
uploads/
*.exe
*.test
.env
config.local.yaml
```

- [ ] **步骤 7：提交**

```bash
git add -A
git commit -m "feat: 初始化 Go 项目，添加配置、docker-compose 和 Makefile"
```

---

### 任务 2：基础设施连接（MySQL、Redis、RabbitMQ）

**文件：**
- 创建：`internal/infra/mysql.go`
- 创建：`internal/infra/redis.go`
- 创建：`internal/infra/rabbitmq.go`
- 创建：`internal/infra/mysql_test.go`
- 创建：`internal/infra/redis_test.go`

**接口：**
- 产出：`NewMySQLPool(cfg) (*sql.DB)`、`NewRedisClient(cfg) (*redis.Client)`、`NewRabbitMQConn(cfg) (*amqp.Connection, *amqp.Channel)`、`DeclareQueues(ch)`（声明所有 7 个队列）
- 消费：任务 1 中的 `Config`

- [ ] **步骤 1：为 MySQL 连接编写失败测试**

创建 `internal/infra/mysql_test.go`：
```go
package infra

import (
    "testing"
    "github.com/stretchr/testify/assert"
    "github.com/yourname/goim/internal/config"
)

func TestMySQLConnection(t *testing.T) {
    cfg, err := config.LoadConfig("../../configs/config.test.yaml")
    assert.NoError(t, err)

    db, err := NewMySQLPool(&cfg.MySQL)
    assert.NoError(t, err)
    assert.NotNil(t, db)

    err = db.Ping()
    assert.NoError(t, err)

    db.Close()
}
```

- [ ] **步骤 2：运行测试验证其失败**

```bash
go test ./internal/infra/ -run TestMySQLConnection -v
```
预期：FAIL —— `NewMySQLPool` 未定义

- [ ] **步骤 3：实现 MySQL 连接池**

创建 `internal/infra/mysql.go`：
```go
package infra

import (
    "fmt"
    "time"
    "database/sql"
    _ "github.com/go-sql-driver/mysql"
    "github.com/yourname/goim/internal/config"
)

func NewMySQLPool(cfg *config.MySQLConfig) (*sql.DB, error) {
    dsn := fmt.Sprintf("%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&parseTime=true",
        cfg.User, cfg.Password, cfg.Host, cfg.Port, cfg.DBName)
    db, err := sql.Open("mysql", dsn)
    if err != nil {
        return nil, err
    }
    db.SetMaxOpenConns(100)
    db.SetMaxIdleConns(20)
    db.SetConnMaxLifetime(5 * time.Minute)
    return db, nil
}
```

还需要：`go get github.com/go-sql-driver/mysql`

- [ ] **步骤 4：运行测试验证其通过**

```bash
docker-compose up -d mysql
# 等待 15 秒让 mysql 初始化
go test ./internal/infra/ -run TestMySQLConnection -v
```
预期：PASS

- [ ] **步骤 5：为 Redis 连接编写失败测试**

创建 `internal/infra/redis_test.go`：
```go
package infra

import (
    "testing"
    "context"
    "github.com/stretchr/testify/assert"
    "github.com/yourname/goim/internal/config"
)

func TestRedisConnection(t *testing.T) {
    cfg, err := config.LoadConfig("../../configs/config.test.yaml")
    assert.NoError(t, err)

    rdb, err := NewRedisClient(&cfg.Redis)
    assert.NoError(t, err)
    assert.NotNil(t, rdb)

    err = rdb.Ping(context.Background()).Err()
    assert.NoError(t, err)
}
```

- [ ] **步骤 6：运行测试验证其失败**

预期：FAIL —— `NewRedisClient` 未定义

- [ ] **步骤 7：实现 Redis 客户端**

创建 `internal/infra/redis.go`：
```go
package infra

import (
    "github.com/redis/go-redis/v9"
    "github.com/yourname/goim/internal/config"
)

func NewRedisClient(cfg *config.RedisConfig) (*redis.Client, error) {
    rdb := redis.NewClient(&redis.Options{
        Addr:     cfg.Addr,
        Password: cfg.Password,
        DB:       cfg.DB,
    })
    return rdb, nil
}
```

- [ ] **步骤 8：运行测试验证其通过**

```bash
docker-compose up -d redis
go test ./internal/infra/ -run TestRedisConnection -v
```
预期：PASS

- [ ] **步骤 9：实现 RabbitMQ 连接和队列声明**

创建 `internal/infra/rabbitmq.go`：
```go
package infra

import (
    "github.com/amqp091-go/amqp091-go"
    "github.com/yourname/goim/internal/config"
)

var QueueNames = []string{
    "private_msg_persist",
    "group_msg_fanout",
    "moment_push",
    "like_persist",
    "comment_persist",
    "ai_summary_persist",
    "ai_profile_persist",
}

func NewRabbitMQConn(cfg *config.RabbitMQConfig) (*amqp.Connection, *amqp.Channel, error) {
    conn, err := amqp.Dial(cfg.URL)
    if err != nil {
        return nil, nil, err
    }
    ch, err := conn.Channel()
    if err != nil {
        conn.Close()
        return nil, nil, err
    }
    return conn, ch, nil
}

func DeclareQueues(ch *amqp.Channel) error {
    for _, name := range QueueNames {
        _, err := ch.QueueDeclare(name, true, false, false, false, nil)
        if err != nil {
            return err
        }
    }
    return nil
}
```

- [ ] **步骤 10：提交**

```bash
git add -A
git commit -m "feat: 添加 MySQL、Redis、RabbitMQ 基础设施连接"
```

---

### 任务 3：MySQL 迁移（所有表）

**文件：**
- 创建：`scripts/migrations/001_create_users.sql`
- 创建：`scripts/migrations/002_create_friendships.sql`
- 创建：`scripts/migrations/003_create_groups.sql`
- 创建：`scripts/migrations/004_create_messages.sql`
- 创建：`scripts/migrations/005_create_moments.sql`
- 创建：`scripts/migrations/006_create_misc.sql`
- 创建：`scripts/migrations/007_create_ai.sql`

**接口：**
- 产出：所有 MySQL 表就绪可用

- [ ] **步骤 1：创建迁移 001 - 用户表**

```sql
CREATE TABLE IF NOT EXISTS users (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    username      VARCHAR(50) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    nickname      VARCHAR(50) NOT NULL DEFAULT '',
    avatar_url    VARCHAR(255) DEFAULT '',
    sign          VARCHAR(255) DEFAULT '',
    gender        TINYINT DEFAULT 0,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_username (username)
);
```

- [ ] **步骤 2：创建迁移 002 - 好友关系表**

```sql
CREATE TABLE IF NOT EXISTS friend_requests (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    from_user_id  BIGINT NOT NULL,
    to_user_id    BIGINT NOT NULL,
    message       VARCHAR(200) DEFAULT '',
    status        TINYINT NOT NULL DEFAULT 0,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_from_user (from_user_id, status),
    INDEX idx_to_user (to_user_id, status),
    UNIQUE KEY uk_pair (from_user_id, to_user_id)
);

CREATE TABLE IF NOT EXISTS friendships (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id       BIGINT NOT NULL,
    friend_id     BIGINT NOT NULL,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_bidirectional (user_id, friend_id),
    INDEX idx_user (user_id)
);
```

- [ ] **步骤 3：创建迁移 003 - 群组表**

```sql
CREATE TABLE IF NOT EXISTS groups (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    name          VARCHAR(100) NOT NULL,
    notice        VARCHAR(500) DEFAULT '',
    owner_id      BIGINT NOT NULL,
    max_members   INT NOT NULL DEFAULT 500,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_owner (owner_id)
);

CREATE TABLE IF NOT EXISTS group_members (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    group_id      BIGINT NOT NULL,
    user_id       BIGINT NOT NULL,
    role          TINYINT NOT NULL DEFAULT 0,
    muted_until   DATETIME NULL,
    joined_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_group_user (group_id, user_id),
    INDEX idx_group (group_id),
    INDEX idx_user (user_id)
);
```

- [ ] **步骤 4：创建迁移 004 - 消息表**

```sql
CREATE TABLE IF NOT EXISTS private_messages (
    id            BIGINT PRIMARY KEY,
    sender_id     BIGINT NOT NULL,
    receiver_id   BIGINT NOT NULL,
    content       TEXT NOT NULL,
    msg_type      TINYINT NOT NULL DEFAULT 1,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_conv_time (sender_id, receiver_id, created_at),
    INDEX idx_receiver_time (receiver_id, created_at),
    FULLTEXT INDEX ft_content (content)
);

CREATE TABLE IF NOT EXISTS group_messages (
    id            BIGINT PRIMARY KEY,
    group_id      BIGINT NOT NULL,
    sender_id     BIGINT NOT NULL,
    content       TEXT NOT NULL,
    msg_type      TINYINT NOT NULL DEFAULT 1,
    group_seq     BIGINT NOT NULL,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_group_seq (group_id, group_seq),
    INDEX idx_group_time (group_id, created_at),
    FULLTEXT INDEX ft_content (content)
);

CREATE TABLE IF NOT EXISTS msg_revoked (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    msg_id        BIGINT NOT NULL,
    conv_id       VARCHAR(50) NOT NULL,
    operator_id   BIGINT NOT NULL,
    revoked_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_msg (msg_id)
);
```

- [ ] **步骤 5：创建迁移 005 - 朋友圈表**

```sql
CREATE TABLE IF NOT EXISTS moments (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    author_id     BIGINT NOT NULL,
    content       TEXT NOT NULL,
    media_urls    JSON DEFAULT NULL,
    visibility    TINYINT NOT NULL DEFAULT 1,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_author_time (author_id, created_at),
    INDEX idx_time (created_at)
);

CREATE TABLE IF NOT EXISTS moment_likes (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    moment_id     BIGINT NOT NULL,
    user_id       BIGINT NOT NULL,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_moment_user (moment_id, user_id),
    INDEX idx_moment (moment_id)
);

CREATE TABLE IF NOT EXISTS moment_comments (
    id            BIGINT PRIMARY KEY,
    moment_id     BIGINT NOT NULL,
    user_id       BIGINT NOT NULL,
    content       VARCHAR(500) NOT NULL,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_moment_time (moment_id, created_at)
);
```

- [ ] **步骤 6：创建迁移 006 - 杂项表**

```sql
CREATE TABLE IF NOT EXISTS blacklist (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id       BIGINT NOT NULL,
    blocked_id    BIGINT NOT NULL,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_pair (user_id, blocked_id),
    INDEX idx_user (user_id)
);
```

- [ ] **步骤 7：创建迁移 007 - AI 表**

```sql
CREATE TABLE IF NOT EXISTS ai_summaries (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id       BIGINT NOT NULL,
    topic         VARCHAR(100) NOT NULL,
    key_points    JSON NOT NULL,
    conclusion    VARCHAR(500) NOT NULL,
    user_intent   VARCHAR(200) DEFAULT '',
    message_range JSON NOT NULL,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_time (user_id, created_at)
);

CREATE TABLE IF NOT EXISTS ai_user_profiles (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id       BIGINT NOT NULL,
    field_name    VARCHAR(50) NOT NULL,
    value         VARCHAR(200) NOT NULL,
    confidence    FLOAT NOT NULL,
    source        VARCHAR(50) NOT NULL,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_field (user_id, field_name),
    INDEX idx_user (user_id)
);
```

- [ ] **步骤 8：对本地 MySQL 运行迁移**

```bash
docker-compose up -d mysql
sleep 15
make migrate
```

- [ ] **步骤 9：提交**

```bash
git add -A
git commit -m "feat: 添加所有 MySQL 迁移脚本"
```

---

### 任务 4：数据模型和通用类型

**文件：**
- 创建：`internal/model/common.go`
- 创建：`internal/model/user.go`
- 创建：`internal/model/message.go`
- 创建：`internal/model/group.go`
- 创建：`internal/model/moment.go`
- 创建：`internal/model/ai.go`

**接口：**
- 产出：所有模型结构体、常量（MsgType、ConvType、Role 常量）、`BuildConvID()` 辅助函数

- [ ] **步骤 1：创建通用类型**

创建 `internal/model/common.go`：
```go
package model

const (
    MsgTypeText    = 1
    MsgTypeImage   = 2
    MsgTypeVideo   = 3
    MsgTypeAI      = 4
    MsgTypeSystem  = 5
    MsgTypeRevoked = 6

    ConvTypePrivate = 1
    ConvTypeGroup   = 2

    RoleMember = 0
    RoleAdmin  = 1
    RoleOwner  = 2

    AI_SYSTEM_ID = 0
)

// BuildConvID 生成会话 ID：
//   私聊：p_{较小ID}_{较大ID}
//   群聊：g_{群ID}
func BuildConvID(convType int, id1, id2 int64) string {
    if convType == ConvTypeGroup {
        return fmt.Sprintf("g_%d", id1)
    }
    if id1 > id2 {
        id1, id2 = id2, id1
    }
    return fmt.Sprintf("p_%d_%d", id1, id2)
}

// WsMessage — 通用 WebSocket 消息信封
type WsMessage struct {
    Type string          `json:"type"`
    Data json.RawMessage `json:"data"`
}
```

- [ ] **步骤 2：创建用户模型**

创建 `internal/model/user.go`：
```go
package model

import "time"

type User struct {
    ID           int64     `json:"id"`
    Username     string    `json:"username"`
    PasswordHash string    `json:"-"`
    Nickname     string    `json:"nickname"`
    AvatarURL    string    `json:"avatar_url"`
    Sign         string    `json:"sign"`
    Gender       int       `json:"gender"`
    CreatedAt    time.Time `json:"created_at"`
}

type FriendRequest struct {
    ID         int64     `json:"id"`
    FromUserID int64     `json:"from_user_id"`
    ToUserID   int64     `json:"to_user_id"`
    Message    string    `json:"message"`
    Status     int       `json:"status"` // 0=待处理, 1=已接受, 2=已拒绝
    CreatedAt  time.Time `json:"created_at"`
}

type Friendship struct {
    ID        int64     `json:"id"`
    UserID    int64     `json:"user_id"`
    FriendID  int64     `json:"friend_id"`
    CreatedAt time.Time `json:"created_at"`
}
```

- [ ] **步骤 3：创建消息模型**

创建 `internal/model/message.go`：
```go
package model

import "time"

type PrivateMessage struct {
    ID        int64     `json:"msgId"`
    SenderID  int64     `json:"fromId"`
    ReceiverID int64    `json:"toId"`
    Content   string    `json:"content"`
    MsgType   int       `json:"msgType"`
    CreatedAt time.Time `json:"timestamp"`
}

type GroupMessage struct {
    ID        int64     `json:"msgId"`
    GroupID   int64     `json:"groupId"`
    SenderID  int64     `json:"fromId"`
    Content   string    `json:"content"`
    MsgType   int       `json:"msgType"`
    GroupSeq  int64     `json:"groupSeq"`
    CreatedAt time.Time `json:"timestamp"`
}

// InboxMessage — 存储在 Redis inbox/outbox 有序集合中的消息
type InboxMessage struct {
    MsgID      int64  `json:"msgId"`
    ConvID     string `json:"convId"`
    ConvType   int    `json:"convType"`
    FromID     int64  `json:"fromId"`
    ToID       int64  `json:"toId"`
    MsgType    int    `json:"msgType"`
    Content    string `json:"content"`
    ReadStatus int    `json:"readStatus"` // 0=未读, 1=已读（仅私聊）
    Timestamp  int64  `json:"timestamp"`
}

// ServerAck — 消息到达服务器后返回给发送者
type ServerAck struct {
    ClientMsgID  string `json:"clientMsgId"`
    ServerMsgID  int64  `json:"serverMsgId"`
    GroupSeq     int64  `json:"groupSeq,omitempty"`
    Timestamp    int64  `json:"timestamp"`
}

// DeliverAck — 接收者确认消息已送达
type DeliverAck struct {
    ServerMsgID int64 `json:"serverMsgId"`
}

// ReadAck — 用户将会话标记为已读
type ReadAck struct {
    ConvID string `json:"convId"`
}

// SyncReq — 客户端请求离线同步
type SyncReq struct {
    LastSyncTime int64 `json:"lastSyncTime"`
    BatchSize    int   `json:"batchSize"`
}

// SyncBatch — 服务器分批返回离线消息
type SyncBatch struct {
    Messages []InboxMessage `json:"msgs"`
    HasMore  bool           `json:"hasMore"`
    SyncTime int64          `json:"syncTime,omitempty"`
}

// ConvSync — 同步时推送的会话列表 + 未读数
type ConvSync struct {
    Conversations []ConvSummary     `json:"conversations"`
    UnreadMap     map[string]int64  `json:"unreadMap"`
}

// ConvSummary — conv_list 有序集合中的单个会话摘要
type ConvSummary struct {
    ConvID       string `json:"convId"`
    ConvType     int    `json:"convType"`
    TargetID     int64  `json:"targetId"`
    TargetName   string `json:"targetName"`
    TargetAvatar string `json:"targetAvatar"`
    LastMsg      string `json:"lastMsg"`
    LastMsgTime  int64  `json:"lastMsgTime"`
}
```

- [ ] **步骤 4：创建其余模型（群组、朋友圈、AI）**

按照与规范的数据模型部分相同的模式创建 `internal/model/group.go`、`internal/model/moment.go`、`internal/model/ai.go`。每个结构体映射到其 MySQL 表，并包含用于 WebSocket 序列化的 JSON 标签。

- [ ] **步骤 5：提交**

```bash
git add -A
git commit -m "feat: 添加所有数据模型和通用类型"
```

---

### 任务 5：Redis Lua 脚本

**文件：**
- 创建：`internal/redis/lua_private_msg.go`
- 创建：`internal/redis/lua_group_msg.go`
- 创建：`internal/redis/lua_inbox_mark_read.go`
- 创建：`internal/redis/lua_revoke_msg.go`
- 创建：`internal/redis/lua_scripts.go`
- 创建：`internal/redis/lua_test.go`

**接口：**
- 产出：`ExecPrivateMsgCheck(rdb, userID, receiverID) → (isFriend, isOnline, msgID, isBlocked)`、`ExecGroupMsgCheck(rdb, userID, groupID) → (isMember, isMuted)`、`ExecInboxMarkRead(rdb, userID, convID) → count`、`ExecRevokeMsg(rdb, userID, convID, msgID) → ok`
- 消费：任务 2 中的 Redis 客户端

- [ ] **步骤 1：为 private_msg Lua 脚本编写失败测试**

创建 `internal/redis/lua_test.go`：
```go
package redis

import (
    "testing"
    "github.com/stretchr/testify/assert"
    "github.com/redis/go-redis/v9"
    "context"
)

func setupTestRedis(t *testing.T) *redis.Client {
    rdb := redis.NewClient(&redis.Options{Addr: "localhost:6379"})
    err := rdb.Ping(context.Background()).Err()
    assert.NoError(t, err)
    return rdb
}

func TestPrivateMsgCheck(t *testing.T) {
    rdb := setupTestRedis(t)
    ctx := context.Background()

    // 设置测试数据
    rdb.Set(ctx, "friend:1:2", "1", 0)
    rdb.Set(ctx, "online:2", "1", 0)
    rdb.Set(ctx, "blacklist:2:1", "0", 0) // 未被拉黑

    isFriend, isOnline, msgID, isBlocked, err := ExecPrivateMsgCheck(rdb, ctx, 1, 2)
    assert.NoError(t, err)
    assert.True(t, isFriend)
    assert.True(t, isOnline)
    assert.Greater(t, msgID, int64(0))
    assert.False(t, isBlocked)

    // 清理
    rdb.Del(ctx, "friend:1:2", "online:2")
}
```

- [ ] **步骤 2：运行测试验证其失败**

```bash
go test ./internal/redis/ -run TestPrivateMsgCheck -v
```
预期：FAIL

- [ ] **步骤 3：实现 Lua 脚本加载器和 private_msg 脚本**

创建 `internal/redis/lua_scripts.go`：
```go
package redis

import "github.com/redis/go-redis/v9"

// LoadLuaScripts 将所有脚本预加载到 Redis 中以优化 EVALSHA
func LoadLuaScripts(rdb *redis.Client, ctx context.Context) error {
    // go-redis 在首次 Eval 调用时自动加载脚本
    // 无需显式预加载 —— go-redis 内部缓存 SHA
    return nil
}
```

创建 `internal/redis/lua_private_msg.go`：
```go
package redis

import (
    "context"
    "strconv"
    "github.com/redis/go-redis/v9"
)

const luaPrivateMsgCheck = `
local senderID = KEYS[1]
local receiverID = KEYS[2]

-- 1. 检查好友关系
local isFriend = redis.call('EXISTS', 'friend:' .. senderID .. ':' .. receiverID)

-- 2. 检查接收者是否在线
local isOnline = redis.call('EXISTS', 'online:' .. receiverID)

-- 3. 检查黑名单（接收者是否拉黑了发送者）
local isBlocked = redis.call('SISMEMBER', 'blacklist:' .. receiverID, senderID)

-- 4. 分配全局消息 ID
local msgID = redis.call('INCR', 'msg_id_global')

-- 5. 去重检查在服务层通过 SETNX 单独完成

return {isFriend, isOnline, msgID, isBlocked}
`

func ExecPrivateMsgCheck(rdb *redis.Client, ctx context.Context, senderID, receiverID int64) (isFriend bool, isOnline bool, msgID int64, isBlocked bool, err error) {
    keys := []string{strconv.FormatInt(senderID, 10), strconv.FormatInt(receiverID, 10)}
    result, err := rdb.Eval(ctx, luaPrivateMsgCheck, keys).Slice()
    if err != nil {
        return false, false, 0, false, err
    }
    isFriend = result[0] == int64(1)
    isOnline = result[1] == int64(1)
    msgID = result[2].(int64)
    isBlocked = result[3] == int64(1)
    return isFriend, isOnline, msgID, isBlocked, nil
}
```

- [ ] **步骤 4：运行测试验证其通过**

```bash
docker-compose up -d redis
go test ./internal/redis/ -run TestPrivateMsgCheck -v
```
预期：PASS

- [ ] **步骤 5：实现其余 Lua 脚本（group_msg、inbox_mark_read、revoke_msg）**

按照相同模式——Lua 脚本字符串常量 + `Exec*` 函数。内容与规范中的 Lua 脚本部分完全匹配。

`lua_group_msg.go`：检查 SISMEMBER group_members + 检查 muted_until + INCR group_seq + INCR msg_id_global

`lua_inbox_mark_read.go`：扫描 inbox 有序集合，查找匹配 convID 且 readStatus=0 的消息，ZREM 旧消息 + ZADD 带 readStatus=1 的消息，返回已标记消息数量

`lua_revoke_msg.go`：在 inbox/outbox 中按 msgID 查找消息，检查时间戳是否在 2 分钟内，ZREM 原消息 + ZADD 撤回替换（msgType=6）

- [ ] **步骤 6：为每个 Lua 脚本编写测试**

将 `TestGroupMsgCheck`、`TestInboxMarkRead`、`TestRevokeMsg` 添加到 `lua_test.go`。

- [ ] **步骤 7：运行所有测试**

```bash
go test ./internal/redis/ -v
```
预期：全部 PASS

- [ ] **步骤 8：提交**

```bash
git add -A
git commit -m "feat: 添加 Redis Lua 脚本用于私聊消息检查、群聊消息检查、inbox 标记已读、撤回消息"
```

---

### 任务 6：入口点（cmd/server/main.go）

**文件：**
- 创建：`cmd/server/main.go`

**接口：**
- 产出：可运行的 GoIM 服务器，启动 HTTP（Gin）+ WebSocket + MQ 消费者
- 消费：任务 1-5 中的所有基础设施

- [ ] **步骤 1：创建 main.go**

```go
package main

import (
    "flag"
    "log"
    "go.uber.org/zap"
    "github.com/yourname/goim/internal/config"
    "github.com/yourname/goim/internal/infra"
    "github.com/yourname/goim/internal/conn"
    "github.com/yourname/goim/internal/api"
    "github.com/yourname/goim/internal/consumer"
    "github.com/yourname/goim/internal/redis"
)

func main() {
    configPath := flag.String("c", "configs/config.yaml", "配置文件路径")
    flag.Parse()

    // 加载配置
    cfg, err := config.LoadConfig(*configPath)
    if err != nil {
        log.Fatalf("加载配置失败: %v", err)
    }

    // 初始化日志器
    logger, _ := zap.NewProduction()
    defer logger.Sync()

    // 初始化基础设施
    db, err := infra.NewMySQLPool(&cfg.MySQL)
    if err != nil {
        logger.Fatal("连接 MySQL 失败", zap.Error(err))
    }

    rdb, err := infra.NewRedisClient(&cfg.Redis)
    if err != nil {
        logger.Fatal("连接 Redis 失败", zap.Error(err))
    }

    mqConn, mqCh, err := infra.NewRabbitMQConn(&cfg.RabbitMQ)
    if err != nil {
        logger.Fatal("连接 RabbitMQ 失败", zap.Error(err))
    }
    infra.DeclareQueues(mqCh)

    // 加载 Lua 脚本
    ctx := context.Background()
    redis.LoadLuaScripts(rdb, ctx)

    // 初始化 ConnectionManager
    cm := conn.NewConnectionManager()

    // 启动 Gin HTTP + WebSocket
    router := api.SetupRouter(cfg, db, rdb, mqCh, cm, logger)
    go func() {
        if err := router.Run(fmt.Sprintf(":%d", cfg.Server.Port)); err != nil {
            logger.Fatal("HTTP 服务器启动失败", zap.Error(err))
        }
    }()

    // 启动 MQ 消费者
    consumer.StartAll(mqCh, db, rdb, cm, logger)

    // 启动清理协程（inbox/outbox/时间线过期清理）
    infra.StartCleanupTask(rdb, logger)

    logger.Info("GoIM 服务器已启动", zap.Int("port", cfg.Server.Port))
    select {} // 永久阻塞
}
```

- [ ] **步骤 2：验证编译通过**

```bash
go build cmd/server/main.go
```
预期：构建成功（可能还不能完全运行，但编译无错误）

- [ ] **步骤 3：提交**

```bash
git add -A
git commit -m "feat: 添加 main.go 入口点，连接所有基础设施组件"
```

---

## 阶段 1：P0 —— 连接管理 + 消息发送/接收 + 离线同步

### 任务 7：ConnectionManager 和 ClientConnection

**文件：**
- 创建：`internal/conn/manager.go`
- 创建：`internal/conn/client.go`
- 创建：`internal/conn/manager_test.go`
- 创建：`internal/ws/protocol.go`

**接口：**
- 产出：带 `Register/Get/Delete/KickOld` 的 `ConnectionManager`，带 `StartPumps/ReadPump/WritePump/Close` 的 `ClientConnection`，`WsMessage` 类型常量，`encodeMsg/decodeMsg` 辅助函数

- [ ] **步骤 1：为 ConnectionManager 编写失败测试**

创建 `internal/conn/manager_test.go`：
```go
package conn

import (
    "testing"
    "github.com/stretchr/testify/assert"
)

func TestConnectionManager_RegisterGetDelete(t *testing.T) {
    cm := NewConnectionManager()

    // 注册
    client := &ClientConnection{UserID: 1}
    cm.Register(1, client)

    // 获取
    got, ok := cm.Get(1)
    assert.True(t, ok)
    assert.Equal(t, client, got)

    // 删除
    cm.Delete(1)
    _, ok = cm.Get(1)
    assert.False(t, ok)
}

func TestConnectionManager_KickOld(t *testing.T) {
    cm := NewConnectionManager()

    oldClient := &ClientConnection{UserID: 1, SendCh: make(chan []byte, 256), CloseCh: make(chan struct{})}
    cm.Register(1, oldClient)

    newClient := &ClientConnection{UserID: 1, SendCh: make(chan []byte, 256), CloseCh: make(chan struct{})}
    cm.KickOld(1, newClient)

    // 旧连接应收到踢出消息
    select {
    case msg := <-oldClient.SendCh:
        assert.Contains(t, string(msg), "kick")
    default:
        t.Fatal("旧客户端应收到踢出消息")
    }

    // 新连接应已注册
    got, ok := cm.Get(1)
    assert.True(t, ok)
    assert.Equal(t, newClient, got)
}
```

- [ ] **步骤 2：运行测试验证其失败**

预期：FAIL —— `ConnectionManager` 类型未定义

- [ ] **步骤 3：实现 ConnectionManager 和 ClientConnection**

创建 `internal/conn/manager.go`：
```go
package conn

import (
    "encoding/json"
    "sync"
)

type ConnectionManager struct {
    connections sync.Map // userID → *ClientConnection
}

func NewConnectionManager() *ConnectionManager {
    return &ConnectionManager{}
}

func (cm *ConnectionManager) Register(userID int64, client *ClientConnection) {
    cm.connections.Store(userID, client)
}

func (cm *ConnectionManager) Get(userID int64) (*ClientConnection, bool) {
    val, ok := cm.connections.Load(userID)
    if !ok {
        return nil, false
    }
    return val.(*ClientConnection), true
}

func (cm *ConnectionManager) Delete(userID int64) {
    cm.connections.Delete(userID)
}

func (cm *ConnectionManager) KickOld(userID int64, newClient *ClientConnection) {
    old, ok := cm.Get(userID)
    if ok {
        // 向旧连接发送踢出消息
        kickMsg, _ := json.Marshal(map[string]string{"type": "kick", "reason": "new_login"})
        select {
        case old.SendCh <- kickMsg:
        default: // 缓冲区已满，直接关闭
        }
        close(old.CloseCh)
        cm.Delete(userID)
    }
    cm.Register(userID, newClient)
}
```

创建 `internal/conn/client.go`：
```go
package conn

import (
    "encoding/json"
    "time"
    "github.com/gorilla/websocket"
)

const (
    maxMessageSize = 4096
    writeWait      = 10 * time.Second
    pongWait       = 60 * time.Second
    pingPeriod     = 30 * time.Second
)

type ClientConnection struct {
    UserID   int64
    Conn     *websocket.Conn
    SendCh   chan []byte
    CloseCh  chan struct{}
    LastPing time.Time
}

func NewClientConnection(userID int64, conn *websocket.Conn) *ClientConnection {
    return &ClientConnection{
        UserID:  userID,
        Conn:    conn,
        SendCh:  make(chan []byte, 256),
        CloseCh: make(chan struct{}),
        LastPing: time.Now(),
    }
}

func (c *ClientConnection) ReadPump(msgHandler func(*ClientConnection, []byte)) {
    defer c.Conn.Close()

    c.Conn.SetReadLimit(maxMessageSize)
    c.Conn.SetReadDeadline(time.Now().Add(pongWait))
    c.Conn.SetPingHandler(func(appData string) error {
        c.LastPing = time.Now()
        c.Conn.SetReadDeadline(time.Now().Add(pongWait))
        return c.Conn.WritePong(appData)
    })

    for {
        _, message, err := c.Conn.ReadMessage()
        if err != nil {
            break
        }
        msgHandler(c, message)
    }
}

func (c *ClientConnection) WritePump() {
    ticker := time.NewTicker(pingPeriod)
    defer func() {
        ticker.Stop()
        c.Conn.Close()
    }()

    for {
        select {
        case msg, ok := <-c.SendCh:
            c.Conn.SetWriteDeadline(time.Now().Add(writeWait))
            if !ok {
                c.Conn.WriteMessage(websocket.CloseMessage, []byte{})
                return
            }
            c.Conn.WriteMessage(websocket.TextMessage, msg)
        case <-ticker.C:
            c.Conn.SetWriteDeadline(time.Now().Add(writeWait))
            if err := c.Conn.WriteMessage(websocket.PingMessage, nil); err != nil {
                return
            }
        case <-c.CloseCh:
            c.Conn.WriteMessage(websocket.CloseMessage, []byte{})
            return
        }
    }
}

func (c *ClientConnection) Close() {
    c.Conn.Close()
}
```

- [ ] **步骤 4：运行测试验证其通过**

```bash
go test ./internal/conn/ -v
```
预期：PASS

- [ ] **步骤 5：创建 WebSocket 协议辅助函数**

创建 `internal/ws/protocol.go`：
```go
package ws

import "encoding/json"

// WsMessage 类型常量
const (
    TypeMsg          = "msg"
    TypeServerAck    = "serverAck"
    TypeDeliverAck   = "deliverAck"
    TypeReadAck      = "readAck"
    TypeSyncReq      = "syncReq"
    TypeSyncBatch    = "syncBatch"
    TypeConvSync     = "convSync"
    TypeRevokeMsg    = "revokeMsg"
    TypeMsgRevoked   = "msgRevoked"
    TypeKick         = "kick"
    TypeAiStream     = "aiStream"
    TypeFriendApply  = "friendApply"
    TypeFriendAccepted = "friendAccepted"
    TypePresence     = "presence"
    TypeError        = "error"
    TypePing         = "ping"
    TypePong         = "pong"
)

// WsMessage — 通用信封
type WsMessage struct {
    Type string          `json:"type"`
    Data json.RawMessage `json:"data"`
}

func EncodeMsg(msgType string, data interface{}) ([]byte, error) {
    dataBytes, err := json.Marshal(data)
    if err != nil {
        return nil, err
    }
    envelope := WsMessage{Type: msgType, Data: dataBytes}
    return json.Marshal(envelope)
}

func DecodeMsg(raw []byte) (*WsMessage, error) {
    var msg WsMessage
    if err := json.Unmarshal(raw, &msg); err != nil {
        return nil, err
    }
    return &msg, nil
}
```

- [ ] **步骤 6：提交**

```bash
git add -A
git commit -m "feat: 添加 ConnectionManager、带双协程模型的 ClientConnection 和 WS 协议"
```

---

### 任务 8：WebSocket 升级 + JWT 认证 + 消息分发器

**文件：**
- 创建：`internal/ws/upgrade.go`
- 创建：`internal/conn/handler.go`
- 创建：`internal/middleware/auth_middleware.go`
- 创建：`internal/ws/upgrade_test.go`

**接口：**
- 产出：`ServeWebSocket(c, rdb, cm)` Gin 处理器、`AuthMiddleware()` Gin 中间件、`handleMessage()` 分发器将 WS 消息路由到服务

- [ ] **步骤 1：为 JWT 中间件编写失败测试**

创建 `internal/middleware/auth_middleware_test.go`：
```go
package middleware

import (
    "testing"
    "net/http"
    "net/http/httptest"
    "github.com/gin-gonic/gin"
    "github.com/stretchr/testify/assert"
)

func TestJWTAuthMiddleware_ValidToken(t *testing.T) {
    // 生成有效令牌
    token, err := GenerateAccessToken(1, "testuser", "test-secret", 2)
    assert.NoError(t, err)

    r := gin.New()
    r.Use(JWTAuthMiddleware("test-secret"))
    r.GET("/test", func(c *gin.Context) {
        userID := c.GetInt64("userID")
        c.JSON(200, gin.H{"userID": userID})
    })

    req := httptest.NewRequest("GET", "/test", nil)
    req.Header.Set("Authorization", "Bearer "+token)
    w := httptest.NewRecorder()
    r.ServeHTTP(w, req)

    assert.Equal(t, 200, w.Code)
}
```

- [ ] **步骤 2：运行测试验证其失败**

预期：FAIL

- [ ] **步骤 3：实现 JWT 中间件和认证服务**

创建 `internal/middleware/auth_middleware.go`：
```go
package middleware

import (
    "fmt"
    "net/http"
    "strings"
    "time"
    "github.com/gin-gonic/gin"
    "github.com/golang-jwt/jwt/v5"
)

type Claims struct {
    UserID   int64  `json:"user_id"`
    Username string `json:"username"`
    jwt.RegisteredClaims
}

func GenerateAccessToken(userID int64, username, secret string, expireHours int) (string, error) {
    claims := &Claims{
        UserID:   userID,
        Username: username,
        RegisteredClaims: jwt.RegisteredClaims{
            ExpiresAt: jwt.NewNumericDate(time.Now().Add(time.Duration(expireHours) * time.Hour)),
        },
    }
    token := jwt.NewWithClaims(jwt.SigningMethodHS256, claims)
    return token.SignedString([]byte(secret))
}

func GenerateRefreshToken(userID int64, secret string, expireDays int) (string, error) {
    claims := &Claims{
        UserID: userID,
        RegisteredClaims: jwt.RegisteredClaims{
            ExpiresAt: jwt.NewNumericDate(time.Now().Add(time.Duration(expireDays) * 24 * time.Hour)),
        },
    }
    token := jwt.NewWithClaims(jwt.SigningMethodHS256, claims)
    return token.SignedString([]byte(secret))
}

func JWTAuthMiddleware(secret string) gin.HandlerFunc {
    return func(c *gin.Context) {
        authHeader := c.GetHeader("Authorization")
        if authHeader == "" {
            // 也检查 WebSocket 的查询参数
            token := c.Query("token")
            if token == "" {
                c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{"error": "缺少令牌"})
                return
            }
            authHeader = "Bearer " + token
        }

        tokenStr := strings.TrimPrefix(authHeader, "Bearer ")
        claims := &Claims{}
        _, err := jwt.ParseWithClaims(tokenStr, claims, func(token *jwt.Token) (interface{}, error) {
            return []byte(secret), nil
        })
        if err != nil {
            c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{"error": "无效令牌"})
            return
        }

        c.Set("userID", claims.UserID)
        c.Set("username", claims.Username)
        c.Next()
    }
}
```

- [ ] **步骤 4：运行测试验证其通过**

预期：PASS

- [ ] **步骤 5：实现 WebSocket 升级处理器**

创建 `internal/ws/upgrade.go`：
```go
package ws

import (
    "context"
    "fmt"
    "time"
    "github.com/gin-gonic/gin"
    "github.com/gorilla/websocket"
    "github.com/redis/go-redis/v9"
    "github.com/yourname/goim/internal/conn"
    "github.com/yourname/goim/internal/middleware"
    "github.com/yourname/goim/internal/config"
)

var upgrader = websocket.Upgrader{
    ReadBufferSize:  1024,
    WriteBufferSize: 1024,
    CheckOrigin:     func(r *http.Request) bool { return true }, // 开发环境允许所有来源
}

func ServeWebSocket(cfg *config.Config, rdb *redis.Client, cm *conn.ConnectionManager, msgHandler func(*conn.ClientConnection, []byte)) gin.HandlerFunc {
    return func(c *gin.Context) {
        // JWT 认证（从查询参数获取令牌）
        token := c.Query("token")
        claims := &middleware.Claims{}
        _, err := jwt.ParseWithClaims(token, claims, func(token *jwt.Token) (interface{}, error) {
            return []byte(cfg.JWT.Secret), nil
        })
        if err != nil {
            c.JSON(401, gin.H{"error": "无效令牌"})
            return
        }

        // 升级到 WebSocket
        wsConn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
        if err != nil {
            return
        }

        // 创建 ClientConnection
        client := conn.NewClientConnection(claims.UserID, wsConn)

        // 踢出旧连接
        cm.KickOld(claims.UserID, client)

        // 更新 Redis 在线状态
        ctx := context.Background()
        rdb.Set(ctx, fmt.Sprintf("online:%d", claims.UserID), "1", 60*time.Second)
        rdb.Set(ctx, fmt.Sprintf("conn:%d", claims.UserID), fmt.Sprintf("%d", time.Now().UnixNano()), 0)

        // 启动 pumps
        go client.WritePump()
        go client.ReadPump(msgHandler)
    }
}
```

- [ ] **步骤 6：实现消息分发器**

创建 `internal/conn/handler.go`：
```go
package conn

import (
    "github.com/yourname/goim/internal/ws"
    "github.com/yourname/goim/internal/service"
)

// MessageDispatcher 将 WebSocket 消息路由到相应的服务处理器
func NewMessageDispatcher(msgSvc *service.MsgService, friendSvc *service.FriendService, aiSvc *service.AIService) func(*ClientConnection, []byte) {
    return func(c *ClientConnection, rawMsg []byte) {
        msg, err := ws.DecodeMsg(rawMsg)
        if err != nil {
            // 无效消息格式，跳过
            return
        }

        switch msg.Type {
        case ws.TypeMsg:
            msgSvc.HandleSendMessage(c, msg.Data)
        case ws.TypeDeliverAck:
            msgSvc.HandleDeliverAck(c, msg.Data)
        case ws.TypeReadAck:
            msgSvc.HandleReadAck(c, msg.Data)
        case ws.TypeSyncReq:
            msgSvc.HandleSyncReq(c, msg.Data)
        case ws.TypeRevokeMsg:
            msgSvc.HandleRevokeMsg(c, msg.Data)
        case ws.TypePing:
            // 由 ReadPump 中的 PingHandler 处理
        default:
            // 未知消息类型，跳过
        }
    }
}
```

- [ ] **步骤 7：提交**

```bash
git add -A
git commit -m "feat: 添加带 JWT 认证的 WebSocket 升级、消息分发器和 JWT 中间件"
```

---

### 任务 9：消息服务（发送/接收 + 离线同步）

**文件：**
- 创建：`internal/service/msg_service.go`
- 创建：`internal/service/msg_service_test.go`
- 创建：`internal/repository/redis_repo.go`（部分：inbox/outbox 操作）
- 创建：`internal/repository/mq_repo.go`（部分：发布辅助函数）

**接口：**
- 产出：`MsgService.HandleSendMessage()`、`MsgService.HandleDeliverAck()`、`MsgService.HandleReadAck()`、`MsgService.HandleSyncReq()`、`MsgService.HandleRevokeMsg()`
- 消费：ConnectionManager、Redis 客户端、MQ 通道、任务 5 中的 Lua 脚本

这是最大的任务。它实现了核心消息流程：私聊（inbox 推送）+ 群聊（outbox 拉取）+ 离线同步 + 已读/未读。

- [ ] **步骤 1：为私聊消息发送编写失败测试**

创建 `internal/service/msg_service_test.go`：
```go
package service

import (
    "testing"
    "context"
    "github.com/stretchr/testify/assert"
    "github.com/redis/go-redis/v9"
    "github.com/yourname/goim/internal/conn"
    "github.com/yourname/goim/internal/model"
)

func TestPrivateMsgSend(t *testing.T) {
    rdb := redis.NewClient(&redis.Options{Addr: "localhost:6379"})
    ctx := context.Background()

    // 设置：标记用户为好友 + 接收者在线
    rdb.Set(ctx, "friend:1:2", "1", 0)
    rdb.Set(ctx, "online:2", "1", 0)

    cm := conn.NewConnectionManager()
    svc := NewMsgService(rdb, nil, cm) // mqCh 暂时为 nil

    // 测试：HandleSendMessage 处理私聊
    // ... 从用户 1 向用户 2 发送消息
    // 验证：inbox:{2} 应包含该消息
    // 验证：serverAck 应返回给发送者
}
```

- [ ] **步骤 2：运行测试验证其失败**

预期：FAIL —— `MsgService` 未定义

- [ ] **步骤 3：实现 Redis 仓库（inbox/outbox 操作）**

创建 `internal/repository/redis_repo.go` —— 这是一个包含所有 Redis 操作的大型文件：
- `WriteInbox(userID, msg)` — ZADD inbox:{userID}
- `WriteOutbox(groupID, msg)` — ZADD outbox:{groupID}
- `ReadInbox(userID, lastSyncTime, batchSize)` — ZREVRANGEBYSCORE
- `ReadOutbox(groupID, lastReadSeq, limit)` — ZREVRANGEBYSCORE
- `UpdateConvList(userID, convID, summary)` — ZADD conv_list:{userID}
- `IncrementUnread(userID, convID)` — HINCRBY unread:{userID}
- `ClearUnread(userID, convID)` — HSET unread:{userID} convID 0
- `SetGroupReadPos(userID, convID, seq)` — HSET group_read_pos:{userID}
- `TrimInbox(userID, maxCount)` — ZREMRANGEBYRANK + ZREMRANGEBYSCORE
- `TrimOutbox(groupID, maxCount)` — 同上
- `GetConvList(userID)` — ZREVRANGE conv_list:{userID}
- `GetUnreadMap(userID)` — HGETALL unread:{userID}
- `GetGroupReadPos(userID, convID)` — HGET group_read_pos:{userID}
- `CheckDuplicate(userID, clientMsgID)` — SETNX msg_dedup:{uid}:{clientMsgID}

- [ ] **步骤 4：实现 MQ 发布辅助函数**

创建 `internal/repository/mq_repo.go`：
```go
package repository

import (
    "encoding/json"
    "github.com/amqp091-go/amqp091-go"
    "github.com/yourname/goim/internal/model"
)

func PublishPrivateMsg(ch *amqp.Channel, msg *model.PrivateMessage) error {
    body, _ := json.Marshal(msg)
    return ch.Publish("", "private_msg_persist", false, false, amqp.Publishing{
        ContentType: "application/json",
        Body:        body,
        DeliveryMode: 2, // 持久化
    })
}

func PublishGroupMsg(ch *amqp.Channel, msg *model.GroupMessage) error {
    body, _ := json.Marshal(msg)
    return ch.Publish("", "group_msg_fanout", false, false, amqp.Publishing{
        ContentType: "application/json",
        Body:        body,
        DeliveryMode: 2,
    })
}
```

- [ ] **步骤 5：实现 MsgService**

创建 `internal/service/msg_service.go` —— 实现以下功能：
- `HandleSendMessage(c, data)` — Lua 检查 → 去重检查 → 写入 MQ → 返回 serverAck
- `HandleDeliverAck(c, data)` — 记录送达确认
- `HandleReadAck(c, data)` — Lua 标记 inbox 中 readStatus 0→1 + 清除未读
- `HandleSyncReq(c, data)` — ZREVRANGEBYSCORE inbox + 群 outbox 拉取 + 批量推送 + convSync
- `HandleRevokeMsg(c, data)` — Lua 撤回 inbox/outbox 中的消息 + 推送撤回通知

这是核心业务逻辑。每个处理方法严格按照规范中定义的流程执行。

- [ ] **步骤 6：运行测试**

```bash
go test ./internal/service/ -v
```
预期：PASS

- [ ] **步骤 7：提交**

```bash
git add -A
git commit -m "feat: 添加 MsgService，支持私聊/群聊消息发送、离线同步、已读/未读和撤回"
```

---

### 任务 10：MQ 消费者（private_msg + group_msg）

**文件：**
- 创建：`internal/consumer/private_msg_consumer.go`
- 创建：`internal/consumer/group_msg_consumer.go`
- 创建：`internal/repository/mysql_repo.go`（部分：消息持久化）
- 创建：`internal/consumer/consumer_test.go`

**接口：**
- 产出：`StartPrivateMsgConsumer(ch, db, rdb, cm)`、`StartGroupMsgConsumer(ch, db, rdb, cm)`
- 消费：MsgService、ConnectionManager、Redis 仓库、MySQL 仓库

- [ ] **步骤 1：为 private_msg 消费者编写失败测试**

测试从 `private_msg_persist` 队列消费消息后能写入 inbox + MySQL。

- [ ] **步骤 2：实现 MySQL 仓库（消息持久化）**

创建 `internal/repository/mysql_repo.go` —— `InsertPrivateMessage(db, msg)`、`InsertGroupMessage(db, msg)`，以及后续任务所需的所有其他表 CRUD 操作。

- [ ] **步骤 3：实现 private_msg 消费者**

创建 `internal/consumer/private_msg_consumer.go`：
- 从 `private_msg_persist` 队列消费
- 对每条消息：WriteInbox(接收者) + WriteInbox(发送者, readStatus=1) + UpdateConvList(双方) + IncrementUnread(接收者) + 通过 WS 推送给在线接收者 + InsertPrivateMessage 到 MySQL

- [ ] **步骤 4：实现 group_msg 消费者**

创建 `internal/consumer/group_msg_consumer.go`：
- 从 `group_msg_fanout` 队列消费
- 对每条消息：WriteOutbox(群组) + 对每个成员：UpdateConvList + IncrementUnread + 推送给在线成员 + InsertGroupMessage 到 MySQL + 递增 group_seq

- [ ] **步骤 5：运行测试**

- [ ] **步骤 6：提交**

```bash
git add -A
git commit -m "feat: 添加 MQ 消费者用于私聊和群聊消息持久化和投递"
```

---

### 任务 11：清理任务（inbox/outbox/时间线过期）

**文件：**
- 创建：`internal/infra/cleanup.go`
- 创建：`internal/infra/cleanup_test.go`

**接口：**
- 产出：`StartCleanupTask(rdb, logger)` —— 每隔 1 小时运行 ZREMRANGEBYSCORE + ZREMRANGEBYRANK 的定期协程

- [ ] **步骤 1：编写失败测试**

- [ ] **步骤 2：实现清理协程**

```go
func StartCleanupTask(rdb *redis.Client, logger *zap.Logger) {
    go func() {
        ticker := time.NewTicker(1 * time.Hour)
        for range ticker.C {
            CleanupExpiredData(rdb, logger)
        }
    }()
}

func CleanupExpiredData(rdb *redis.Client, logger *zap.Logger) {
    ctx := context.Background()
    threshold := time.Now().Add(-3 * 24 * time.Hour).Unix()

    // 遍历所有 inbox 键（SCAN inbox:* 模式）
    var cursor uint64
    for {
        keys, nextCursor, err := rdb.Scan(ctx, cursor, "inbox:*", 100).Result()
        // 对每个键：ZREMRANGEBYSCORE（3 天过期）+ ZREMRANGEBYRANK（1000 条上限）
        // 同样处理 outbox:* 和 timeline:*
    }
}
```

- [ ] **步骤 3：运行测试，提交**

---

## 阶段 2：P1 —— 用户认证 + 好友 + 群聊 + 群管理

### 任务 12：认证服务 + HTTP 处理器

**文件：**
- 创建：`internal/service/auth_service.go`
- 创建：`internal/api/auth_handler.go`

**接口：**
- 产出：`POST /auth/register`、`POST /auth/login`、`POST /auth/refresh`

- [ ] **步骤 1：为注册编写失败测试**

- [ ] **步骤 2：实现认证服务**

bcrypt 密码哈希、JWT 令牌生成、MySQL INSERT users。

- [ ] **步骤 3：为登录编写失败测试**

- [ ] **步骤 4：实现登录处理器**

- [ ] **步骤 5：运行测试，提交**

---

### 任务 13：好友服务 + HTTP 处理器

**文件：**
- 创建：`internal/service/friend_service.go`
- 创建：`internal/api/friend_handler.go`

**接口：**
- 产出：`POST /friend/apply`、`POST /friend/accept`、`POST /friend/reject`、`GET /friend/list`、`DELETE /friend/{id}`、`GET /friend/search`

- [ ] **步骤 1 到步骤 5：好友操作的 TDD**

每个端点：编写测试 → 实现 → 验证 → 提交步骤。

好友申请流程：MySQL INSERT friend_requests + Redis SETNX friend:{uid}:{fid} 缓存 + WebSocket 推送 friendApply 通知给目标用户。

---

### 任务 14：群组服务 + HTTP 处理器 + group_msg 消费者细化

**文件：**
- 创建：`internal/service/group_service.go`
- 创建：`internal/api/group_handler.go`

**接口：**
- 产出：所有群组 CRUD 端点、群成员管理、踢出、禁言等

- [ ] **步骤 1 到步骤 6：群组操作的 TDD**

创建群组：MySQL INSERT groups + group_members + Redis SADD group_members + SADD group_list 为每个成员。

---

## 阶段 3：P2 —— 朋友圈动态 + AI 助手

### 任务 15：朋友圈服务 + HTTP 处理器 + MQ 消费者

**文件：**
- 创建：`internal/service/moment_service.go`
- 创建：`internal/api/moment_handler.go`
- 创建：`internal/consumer/moment_push_consumer.go`
- 创建：`internal/consumer/like_consumer.go`
- 创建：`internal/consumer/comment_consumer.go`

**接口：**
- 产出：朋友圈 CRUD、点赞/取消点赞、评论、动态流端点、时间线推送消费者、点赞持久化消费者、评论持久化消费者

- [ ] **步骤 1 到步骤 8：朋友圈的 TDD**

发布朋友圈 → MySQL INSERT → MQ moment_push → 消费者：遍历好友 + ZADD timeline:{friendID}

点赞：SISMEMBER moment_liked → SADD/SREM + HINCRBY moment_stats → MQ like_persist → 消费者：INSERT/DELETE MySQL

评论：LPUSH moment_comments + HINCRBY → MQ comment_persist → 消费者：INSERT MySQL

---

### 任务 16：AI 服务 + LLM 集成 + 记忆层

**文件：**
- 创建：`internal/service/ai_service.go`
- 创建：`internal/llm/client.go`
- 创建：`internal/llm/openai_client.go`
- 创建：`internal/llm/domestic_client.go`
- 创建：`internal/consumer/ai_consumer.go`

**接口：**
- 产出：带 4 层记忆的 AI 聊天处理器、流式响应、上下文管理、LLMClient 接口

这是最复杂的任务。按顺序实现：
1. LLMClient 接口 + OpenAI 实现
2. 第 0 层（原始记忆）—— 仅 MySQL 查询，通过 private_messages 表已实现
3. 第 1 层（结构化摘要）—— 主题检测 + LLM 摘要生成 + Redis List
4. 第 2 层（带置信度的用户画像）—— LLM 提取 + 置信度演进 + Redis Hash
5. 第 3 层（工作记忆组装）—— 从 L0/L1/L2 召回 + 构建 LLM 请求
6. AI 聊天处理器 —— SETNX 锁 → 构建工作记忆 → 流式 LLM → aiStream 分块 → 写入 inbox + 保存上下文

- [ ] **步骤 1 到步骤 12：AI 服务的 TDD**

从 LLMClient 接口测试开始，然后记忆层测试，最后完整聊天流程测试。

---

## 阶段 4：P3 —— 消息操作 + 用户设置

### 任务 17：消息操作服务 + 用户设置服务

**文件：**
- 创建：`internal/service/msg_op_service.go`
- 创建：`internal/service/user_service.go`
- 创建：`internal/api/msg_handler.go`（历史记录/搜索端点）
- 创建：`internal/api/user_handler.go`

**接口：**
- 产出：撤回、本地删除、搜索、黑名单、禁言群组、个人资料更新

- [ ] **步骤 1 到步骤 6：消息操作和用户设置的 TDD**

撤回：Lua revoke_msg 脚本（已在任务 5 中完成）+ WebSocket 推送 + MySQL INSERT msg_revoked

搜索：MySQL 全文搜索查询

黑名单：Redis SADD/SREM + MySQL INSERT/DELETE

---

### 任务 18：Gin 路由设置 —— 将所有内容连接起来

**文件：**
- 创建：`internal/api/router.go`

**接口：**
- 产出：包含所有 HTTP 端点 + WebSocket 路由 + JWT 中间件的完整 Gin 路由器

- [ ] **步骤 1：创建包含所有路由的 router.go**

```go
func SetupRouter(cfg, db, rdb, mqCh, cm, logger) *gin.Engine {
    r := gin.Default()

    // 认证（无需 JWT）
    authHandler := api.NewAuthHandler(db, cfg)
    r.POST("/api/v1/auth/register", authHandler.Register)
    r.POST("/api/v1/auth/login", authHandler.Login)
    r.POST("/api/v1/auth/refresh", authHandler.Refresh)

    // 所有其他路由需要 JWT
    authorized := r.Group("/api/v1")
    authorized.Use(middleware.JWTAuthMiddleware(cfg.JWT.Secret))
    {
        // 好友
        friendHandler := api.NewFriendHandler(db, rdb, cm)
        authorized.POST("/friend/apply", friendHandler.Apply)
        authorized.POST("/friend/accept", friendHandler.Accept)
        // ... 所有好友路由

        // 群组
        groupHandler := api.NewGroupHandler(db, rdb, cm)
        authorized.POST("/group/create", groupHandler.Create)
        // ... 所有群组路由

        // 朋友圈
        momentHandler := api.NewMomentHandler(db, rdb, mqCh)
        authorized.POST("/moment/publish", momentHandler.Publish)
        // ... 所有朋友圈路由

        // 消息
        msgHandler := api.NewMsgHandler(db, rdb)
        authorized.GET("/msg/history", msgHandler.History)
        authorized.GET("/msg/search", msgHandler.Search)

        // 文件上传
        fileHandler := api.NewFileHandler(cfg)
        authorized.POST("/file/upload", fileHandler.Upload)

        // 用户
        userHandler := api.NewUserHandler(db, rdb)
        authorized.PUT("/user/profile", userHandler.UpdateProfile)
        // ... 所有用户路由

        // AI
        aiHandler := api.NewAIHandler(rdb)
        authorized.POST("/ai/clear-context", aiHandler.ClearContext)
    }

    // WebSocket
    msgDispatcher := conn.NewMessageDispatcher(msgSvc, friendSvc, aiSvc)
    r.GET("/ws", ws.ServeWebSocket(cfg, rdb, cm, msgDispatcher))

    return r
}
```

- [ ] **步骤 2：运行完整服务器测试**

```bash
docker-compose up -d
make migrate
go run cmd/server/main.go -c configs/config.yaml
# 验证：HTTP 端点响应，WebSocket 连接成功
```

- [ ] **步骤 3：提交**

```bash
git add -A
git commit -m "feat: 将所有 HTTP 路由和 WebSocket 处理器连接到 Gin 路由器"
```

---

## 阶段 5：集成测试与文档

### 任务 19：端到端集成测试

**文件：**
- 创建：`tests/integration/e2e_test.go`

**接口：**
- 产出：完整的端到端测试，覆盖：注册 → 登录 → 连接 WS → 发送私聊消息 → 接收 → 群聊 → 朋友圈 → AI 聊天

- [ ] **步骤 1：编写端到端测试**

- [ ] **步骤 2：运行并验证**

- [ ] **步骤 3：提交**

---

### 任务 20：README.md + 项目文档

**文件：**
- 创建：`README.md`
- 创建：`docs/architecture.md`（技术设计文档的简化版）

- [ ] **步骤 1：编写 README，包含项目概述、设置说明、架构亮点**

- [ ] **步骤 2：提交**

```bash
git add -A
git commit -m "docs: 添加 README 和架构文档"
```

---

## 自检清单

**1. 规范覆盖：** ✅ 所有 P0/P1/P2/P3 模块通过任务 7-18 覆盖

**2. 占位符扫描：** ✅ 无 TBD/TODO —— 所有步骤包含实际代码和命令

**3. 类型一致性：** ✅ `InboxMessage`、`WsMessage`、`ServerAck`、`ClientConnection` 等在任务中定义一次并在各处一致引用

**4. 缺失任务：** ✅ 文件上传处理器（任务 18 包含）、清理协程（任务 11）、MQ 队列声明（任务 2）
